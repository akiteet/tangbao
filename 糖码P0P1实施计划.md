# 糖码 Coding Agent — P0+P1 实施计划（12 项）

> 来源：对照《糖码 Coding Agent 优化方向总计划.md》第 4/5 次审计，范围 P0×4 + P1×8。
> 设计依据：实际读取 agent-server.js(2538) / capabilities.js(199) / context.js(397) / bench.js(237) / tasks.json(27) / .gitignore / sqlite-store.js / main.js / ci.yml 后形成。
> 注意：上下文管理实际位于 `src/renderer/views/chat/context.js`（非 `src/core/context/`）。

## 依赖关系
- P0-3（todo 状态机）→ 产出状态数据 → 被 P0-4（检查点）、P1-9（blockedWork）消费。
- P1-7（统一 ToolResult）→ 是 P1-8（新工具结构）、P1-9（结构化拒绝）的前置。
- P1-10（六区预算）与 P1-11（contextWindow 单一来源）强耦合，同批。
- P1-12（bench）依赖 P1-7/P1-9 落地的指标字段，可放最后。

---

## 批次 A — P0 基础（低风险、高优先）
范围：P0-1、P0-2、P0-3、P0-4

### P0-1 六档权限模式运行时提示
- 文件：`src/infrastructure/agent-runtime/agent-server.js` ~2088
- 现状：仅 `planMode` 一档注入 runtimePolicy；6 档枚举见 `agent.js:121-124`，白名单 `:2036`，`permCtx` `:2041`。
- 改动：基于 `permCtx.permissionMode` 的 6 分支 switch，各自 `addSection('runtimePolicy', 90, <文案>)`；文案表 `PERM_RUNTIME_HINT` 就近常量化；sandbox/bypass 标注危险、bypass 强提示「跳过所有审批」。
- 验证：`node --check`；各模式启动 run 确认 SSE runtimePolicy 文案随模式变化。

### P0-2 临时文件与 .gitignore 治理
- 文件：`.gitignore`
- 现状：仅 `node_modules/ dist/ dist-v107/ *.exe .blockmap .DS_Store Thumbs.db *.log`；缺 `_*.cjs`/`_*.py`/`__pycache__/`*.pyc`。残留 11 个未跟踪临时文件。
- 改动：`.gitignore` 追加 4 条规则；`git clean -n` 预演后 `git clean -f` 安全清理（注意 safe-delete 机制，不暴力 rm）。
- 验证：`git status --porcelain | grep '^??'` 为空。

### P0-3 todo_write 状态机校验（§7.3）
- 文件：`agent-server.js` `todo_write` :1623-1639
- 新增 `validateTodoState(todos, incoming)`：单 in_progress 约束（>1 → `multiple-in-progress`）、依赖门（dependsOn 未全 completed → `dependency-unmet`）、测试失败门（`type:'test'` 上次失败 → `test-fail-gate`）。
- 不通过经 `normalizeResult` 返回 `status:'blocked'`；通过才写 `opts.todos` + `upsertWorkingState`。
- 验证：`node --check`；三类违规输入返回对应 error 码。

### P0-4 检查点补全（abort / 连续失败 / 退出前）
- 文件：`agent-server.js` abort 分支 :2204、连续失败分支 :2280-2284；`main.js` `will-quit` :1255
- 改动：abort break 前 `saveAgentCheckpoint(runId,'aborted',...)`；连续失败 emit blocked 后 `saveAgentCheckpoint(runId,'consecutive-fail',...)`；`before-quit` 中 `preventDefault` + 异步 flush 进行中 run 检查点再 `app.quit()`。
- 验证：abort 后 `getCheckpoint(reason='aborted')` 可取；连续失败出现 `consecutive-fail` 检查点。

---

## 批次 B — P1 工具与结构统一（中风险）
范围：P1-5、P1-6、P1-7

### P1-5 read_file 增加 expectedHash 与 nextStartLine
- 文件：`agent-server.js` `read_file` :1555-1621
- 入参 `expectedHash`：sha256Hex 比对，不一致返回 `hash-mismatch` + actualHash；出参 `nextStartLine`：truncated 时 = endLine+1；经 `normalizeResult` 出口。

### P1-6 新增 read_files / get_file_outline
- `TOOLS` 数组 :98-243 追加两项声明；抽出 `readOneFile(opts)` 复用 read_file 主体；`get_file_outline` 复用 find_symbol 符号提取返回顶层大纲；`runTool` 增两个分支。

### P1-7 统一 9 个工具的 ToolResult 结构
- 文件：`agent-server.js` web_search/find_symbol/find_references/todo_write/grep/write_file/edit_file/list_dir/glob 的 `return <string>` 改为 `return normalizeResult({...})`；保留 `raw` 兜底；统一错误分支为 `normalizeResult({ok:false,error,<code>,message})`。

---

## 批次 C — P1 交互与持久化增强（高风险）
范围：P1-8、P1-9

### P1-8 新增 report_blocker / request_user_decision
- `TOOLS` 追加两项；`report_blocker` 写 `ws.blockedWork` + emit blocked + 返回结构；`request_user_decision` 发 `user-decision-required` SSE 事件并挂起步骤，用户应答后继续，超时返回 `user-decision-timeout`。

### P1-9 拒绝路径持久化
- 新增 `recordDenial(ws,{tool,args,reason,alternativeHint})`：push `ws.blockedWork` + `ws.decisions`（decision:'denied'）+ `upsertWorkingState`；5 处拒绝路径（write_file/edit_file/apply_patch/run_command/git）调用后再返回 deny 结构。

---

## 批次 D — P1 上下文与预算（中风险、强耦合）
范围：P1-10、P1-11

### P1-10 六区 Token 预算（§8.3）
- 文件：`src/renderer/views/chat/context.js` :126-143
- 3 档阈值替换为六区常量（system 8% / history 20% / tool 8% / current 15% / compact-reserve 25% / buffer 24%，合计 1.0）；重构 `agentUtilLevel` 与压缩触发逻辑按六区占用率决策；保留工具结果区独立预算。

### P1-11 capabilities.js 增加 contextWindow 单一来源
- 文件：`src/core/models/capabilities.js` `capsOfModel` :150-183
- 返回对象增 `contextWindow`（按模型填表）；散落点（state.js:46 / context.js:147 / sqlite-store.js:267 / migrator.js / ui.js / agent.js:1496 等）改为调用 `contextWindowOf(model)`；未命中回退默认值。

---

## 批次 E — P1 评测闭环（独立可并行）
范围：P1-12

### P1-12 Bench 扩展至 30 任务 + 4 项指标
- 文件：`benchmarks/tasks.json`（21→30，补足 §19.2 分布，safety/context 正确打 tags）、`scripts/bench.js`（`runEvalTask` :55 扩采 `consecutiveFailures`/`resultTokens`/`regression`/`compressionLoss`；`mainEval` 汇总增聚合；`mainEval` :125 过滤逻辑对齐 §19.2）、`ci.yml` eval job 增指标门槛。
- 验证：`node scripts/bench.js --list` 任务数=30；`RUN_EVAL=true node scripts/bench.js` 输出含 4 项新指标。

---

## 全局注意点
1. 跨端一致性：P0-1 文案、P1-7 `raw` 兜底、P1-8 前端交互 UI 需与 `agent.js` 协同，前端改动另立子任务。
2. CI 护栏建议：临时文件护栏、TOOLS 名称断言、capabilities.js 导出断言、eval 指标门槛，巩固不回退。
3. 本计划为只读产物，改动待批准后由编码阶段实施。
