# 糖包 v1.1.5 实施计划（17 项 / 6 批）

> 来源：2026-08-16 对 v1.1.4（master @ release: publish v1.1.4）的全量评估，覆盖代码规模、重复实现、遗留资产、测试体系、发布工程五个维度。
> 主题：**架构减重 · 发布自动化 · Prompt Caching 接入**（均衡混合：工程健康约 70% + 小功能约 30%）。
> 设计依据：实际读取 package.json / check-version.js / .gitignore / CHANGELOG-v1.1.4.md / agent-runtime-engine.js(4158) / main.js(2900) 后形成；行号以 v1.1.4 工作树为准。
> 上游状态：《糖码P0P1实施计划.md》12 项与 docs/PROMPT_SYSTEM.md 的 P0/P1 建议均已落地，本计划无历史欠账，全部为新立方向。

## 一、项目评估摘要（v1.1.4 现状）

**健康面（保持，不再投入）**
- 464 个测试 8.5s 跑完、0 失败；src/ 零 TODO/FIXME 债务；8 项发布门禁 + Node 18/20/22 × Win/macOS(双架构) CI 矩阵。
- 领域逻辑已较好分层：src/core/agent-runtime/ 已有 21 个领域模块（state-machine、budget-manager、tool-registry 等）。

**债务面（本计划的依据）**
1. 巨石文件：前 5 大文件合计约 15,500 行、占 src 总量约 45%——engine 4158 / agent.js 3711 / main.js 2900 / ui.js 2706 / chat.js 2039。
2. 安全敏感双份代码：`tokenEqual/checkToken/isLoopbackHost` 同时存在于 main.js:422-444 与 agent-runtime-engine.js:411-425，两处需同步修改。
3. 发版摩擦：版本号散布约 11 处（package.json、package-lock 两处、role-registry、engine 双常量、macos-release 两个 workflow、双 README、check-version.js EXPECTED、check-release.js），每次手工编辑。
4. 测试结构性缺口：better-sqlite3 按 Electron 31 ABI 编译，纯 Node 下 4 个用例永远跳过（storage-search-metrics.test.js:17/52/84/104），另有 1 个机器绑定路径跳过（eval-task-contracts.test.js:49）。
5. 遗留资产：空的 js/、server/ 目录 + 4 处指向它们的过时注释；CROSS_MODULE.md 描述的是 v1.0.6 旧路径；dist/ 残留 v1.1.1~1.1.3 历史安装包约 340MB。
6. 基础工具重复：readJson ×4、clone ×4、escapeHtml ×2、`$`(getElementById) 每视图一份、IPC invoke 容错包装三处各写一份。

## 依赖关系

- 批次 B（版本自动化）独立，越早落地越省后续每批的发版操作；建议 A → B 先行。
- C1（鉴权收敛）是 D1（HTTP 层抽出）的前置：先有共享模块，抽层时直接引用。
- D1~D4 相互独立但同文件，按序执行避免冲突；D 批是 E 批（渲染层/主进程拆分）的模板与信心中继。
- F1（Prompt Caching）只动 model-gateway 与指标，不依赖 C/D，可与 D 并行。
- F2（SQLite Electron 通道）独立收尾；它验证的存储层正好被 D 批的重构保护。

---

## 批次 A — 清理与卫生（低风险，先行）

### A1 删除空目录与过时注释
- 文件：`js/`、`server/`（均为空、无 git 追踪文件）；`index.html:16`、`src/main/main.js:471/493/612`、`src/renderer/views/chat/chat.js:8`
- 现状：糖码后端已迁至 src/infrastructure/agent-runtime/、渲染层在 src/renderer/，但 4 处注释仍引用旧路径误导读者。
- 改动：删除两个空目录；4 处注释改为指向实际路径（src/renderer/views/documents/pdf-boot.mjs、src/renderer/runtime.js、src/renderer/components/modules.js、src/renderer/views/chat/context.js）。
- 验证：`node --check` 涉及文件；`git status` 无意外变更；全文搜索 `js/`、`server/` 无残留引用。

### A2 .tmp-electron-user-data/ 治理
- 文件：`.gitignore`
- 现状：该目录未被追踪但也没有 ignore 规则，`git status` 随时可能被污染。
- 改动：追加 `.tmp-electron-user-data/` 规则；确认 ui-smoke 等脚本对该目录的写入路径不受影响。
- 验证：`git status --porcelain` 不再出现该目录。

### A3 本地磁盘清理（不涉及 git）
- 文件：`dist/`（已被 .gitignore 忽略）
- 现状：残留 v1.1.1、v1.1.2、v1.1.3 安装包 + blockmap + win-unpacked 共约 340MB+，仅 v1.1.4 产物有保留价值（可回滚最近一版）。
- 改动：删除 v1.1.1~1.1.3 产物与对应 blockmap/sha256，保留 1.1.4；可选清理 `.tmp-electron-user-data/` 内的陈旧 profile。
- 验证：`ls dist/` 仅剩 1.1.4 系产物与元数据。

### A4 机器绑定测试路径解耦
- 文件：`test/agent-runtime/eval-task-contracts.test.js:49`
- 现状：硬编码 `C:/Users/18860/.../eval-runs-archive-20260808/...` 绝对路径，换机/CI 必跳过。
- 改动：改为读环境变量 `TANGBAO_EVAL_ARCHIVE_DIR`（未设置时保持现有 skip 语义并在跳过消息中提示变量名）；README 开发节补一句说明。
- 验证：`npm test` 用例数不变；设置变量指向本机存档时用例真实执行。

### A5 CROSS_MODULE.md 归档标注
- 文件：`docs/CROSS_MODULE.md`
- 现状：内容为 v1.0.6 时期 js/、server/ 旧路径的历史安全改造记录，已与现状脱节。
- 改动：文首加归档声明（历史快照、路径以 src/ 分层为准），正文不动；不在本版重写。
- 验证：文档打开即见声明；无其他文件依赖其内容作为现行规范（已确认仅文末有 v1.1.0 增补）。

---

## 批次 B — 版本发布自动化（高杠杆，独立）

### B1 bump-version 脚本：11 处手工编辑 → 1 条命令
- 文件：新增 `scripts/bump-version.js`；调整 `scripts/check-version.js:7`、`scripts/check-release.js`
- 现状：check-version.js 硬编码 `EXPECTED='1.1.4'` 并锚定 6 个文件；engine 有 `TOOL_REGISTRY_VERSION`(:368) 与 `RUNTIME_VERSION`(:384) 双常量；两个 workflow 共 3 处 `default: v1.1.4`（macos-release.yml:14、macos-release-assets.yml:9/14）；README 两份各含下载链接 `tangbao-1.1.4-setup.exe`；check-release.js 有 7 行版本断言（:20/:25/:35/:36/:37），其中还检查 `docs/CHANGELOG-v1.1.4.md` 文件名与 DATA_MODEL.md 内容。发版一次需手工改约 11 处、新建 1 个 CHANGELOG 文件。
- 改动：
  1. check-version.js 的 EXPECTED 改为从 package.json 读取（校验器与版本源合一）；
  2. bump-version.js 接收目标版本号，**只替换显式锚点清单**：package.json `version`、package-lock.json 根版本与 `packages[""].version`、role-registry `version:`、engine 双常量、两个 workflow 的 `default:`、README 两份中的 `tangbao-<旧>-setup.exe` 下载/产物链接、check-version/check-release 自身常量；同时提示（或以模板生成）`docs/CHANGELOG-v1.1.5.md`，因为 check-release.js:25 断言该文件存在；
  3. **红线**：不得触碰 README/CHANGELOG 中的历史版本段落（`## v1.1.4` 等标题与正文一律不动），锚点全部使用「前缀 + 旧版本号」的正则精确定位；替换后自动调用 check-version 验证。
- 验证：`node scripts/bump-version.js 1.1.5 --dry-run` 输出拟变更清单；实跑后 `npm run check:version` 通过；`git diff` 中历史段落零变更。

---

## 批次 C — 基础设施收敛（中风险）

### C1 HTTP 鉴权助手收敛（安全敏感，优先）
- 文件：`src/main/main.js:422-444`、`src/infrastructure/agent-runtime/agent-runtime-engine.js:411-425`
- 现状：`tokenEqual`（定时安全比较）、`checkToken`、`isLoopbackHost` 双端各自实现，已出现轻微漂移（行数与实现细节不完全一致），属于最不该重复的代码。
- 改动：新建 `src/infrastructure/http/request-auth.js` 导出三者；main.js 与 engine 改为引用；实现以更严格的一版为准（比对 engine 版 `checkToken` 语义），并补最小单元测试（tokenEqual 不等长/等长、loopback 判定 ::1/127.0.0.1/hostname）。
- 验证：新增测试通过；两处调用点 `node --check`；`npm run check:ui`（主进程静态服务仍正常）。

### C2 基础工具函数去重
- 文件：`src/infrastructure/secrets/legacy-context.js:6`、`src/infrastructure/tangguan/tangguan-store.js:11`、`src/infrastructure/tangguan/embedding-index.js:9`、`src/infrastructure/tangguan/keyword-index.js:9`（readJson）；`src/core/models/image-capabilities.js:34`、`src/core/agent-runtime/tool-registry.js:9`、`src/infrastructure/storage/module-sessions.js:24` 等（clone）；`src/renderer/components/search.js:5` 与 `markdown.js:6`（escapeHtml）
- 改动：Node 侧收敛到 `src/infrastructure/util/shared.js`（readJson/clone）；渲染侧 escapeHtml 统一用 `App.escapeHtml`（markdown.js 导出已存在），search.js 删私有副本；同名分层文件（core/infra 的 tangguan-store 等）为有意设计，**不改名**、仅在文件头补一句分层说明。
- 验证：`npm test` 全绿；全局搜索确认重复定义清零。

### C3 IPC invoke 包装统一
- 文件：`src/application/services/fs.js:27`、`skills.js:5`、`module-sessions.js:17`（fs.js 内 31 处直接 window.electron 调用）
- 改动：新增 `src/application/services/ipc.js` 导出 `invoke(channel, ...args)`（含 electron 桥不存在时的统一降级/报错语义，语义取现三处中最完整的一版）；三个服务全部改走该入口。
- 验证：`npm test` 全绿；`npm run check:ui` 四窗口冒烟通过（渲染层加载路径未被破坏）。

---

## 批次 D — agent-runtime-engine.js 拆分（高风险，分四步，行为零变化）

> 总原则：纯搬运重构。每步只做「抽出到新文件 + engine 引用」，不改任何逻辑、文案、错误码；每步合入后 `npm test` + `npm run bench:offline`（确定性校验，seed 1337 输出必须字节一致）必须全绿再进下一步。目标：engine 从 4158 行降至 <1500 行。

### D1 抽出 HTTP 服务器层
- 文件：`agent-runtime-engine.js:403-465`（configureAgentServer、tokenEqual/cors/sendJSON 等）及散落的 SSE 胶水
- 改动：新建 `src/infrastructure/agent-runtime/agent-server-http.js` 承载服务器创建、鉴权（引用 C1 共享模块）、CORS、SSE 发送助手；engine 保留路由注册与业务处理函数。`agent-server.js`（33 行薄入口）对外行为不变。
- 验证：`npm run server` 可启动；`npm test`（含 m1-loop-and-render、platform-v112 系列）全绿。

### D2 迁移 LEGACY_TOOL_DEFINITIONS
- 文件：`agent-runtime-engine.js:132-368`（`LEGACY_TOOL_DEFINITIONS` 数组，名实不符：实为现行工具集声明）
- 改动：迁入已存在的 `tool-runtime.js`（当前仅 52 行），随迁移正名为 `TOOL_DEFINITIONS`；`TOOL_REGISTRY_VERSION`(:368)、`WRITE_TOOL_NAMES`(:369) 一并归位；对外导出的 TOOLS/TOOL_NAMES 不变。
- 验证：`npm test` 中工具契约类用例（b7、skill-runtime 等）全绿；tools.json 形状的静态断言（ci.yml 8 项锚点之一）不回退。

### D3 抽出搜索供应商
- 文件：`agent-runtime-engine.js:649-720`（fetchWithTimeout(:649) / searchByTavily(:666) / parseBing(:683) / searchByBing(:713)）
- 改动：新建 `src/infrastructure/agent-runtime/search-providers.js`，导出统一 `webSearch(query, {provider, apiKey, timeout})`；engine 的 web_search 工具分支仅编排与结果归一化。
- 验证：新增供应商选择/降级顺序的最小单测；`npm test` 全绿。

### D4 运行态注册表模块化
- 文件：`agent-runtime-engine.js:83-94`（approvals / decisionsPending / jobs / approvedFiles / runAuthRegistry / sessions 六个模块级 Map）及 killTree/killRunJobs/killRunSessions(:97-130)
- 改动：新建 `src/infrastructure/agent-runtime/run-registry.js` 封装六个 Map 的生命周期（register/cleanup/killTree），engine 通过接口访问；abort 路径行为不变。
- 验收（批次整体）：engine <1500 行；464 测试全绿；`check:ui`、`bench:offline` 输出与拆分前一致。

---

## 批次 E — 渲染层/主进程拆分（stretch：视 D 批进度，可整体推 v1.1.6）

### E1 main.js 按职责拆分
- 文件：`src/main/main.js`（2900 行）
- 改动：按窗口管理 / 静态文件服务 / workspace 登记 / 状态持久化 / 浮窗与子窗口 / 审计 六域拆为 src/main/ 下多模块，main.js 收敛为装配入口（<300 行）；沿用 D 批「纯搬运 + 每步门禁」方法。
- 验证：`npm run check:ui` 四窗口 + `check:electron-abi`；`npm test` 全绿。

### E2 agent.js 按面板拆分
- 文件：`src/renderer/views/agent/agent.js`（3711 行、106 处 getElementById）
- 改动：先抽纯函数区（提示词拼装、状态格式化），再按 SSE 处理 / 权限与审批 UI / todo 面板 / 历史面板拆同目录子模块；index.html 脚本加载顺序同步追加。
- 验证：`npm test` UI 契约用例（ui-layout-consistency 等）全绿；`check:ui` 冒烟。

---

## 批次 F — 功能与测试补强

### F1 Prompt Caching 消费方接入（本版唯一用户可见功能）
- 文件：`src/core/models/capabilities.js:198`（supportsPromptCaching 已动态计算）、`src/infrastructure/model-gateway/adapters.js`（buildRequest/parseNonStream）、`src/infrastructure/model-gateway/gateway.js`（802 行）
- 现状：能力位已就绪但无消费方——请求从不带缓存标记，usage 中的缓存命中也未单独沉淀。
- 改动：
  1. adapters.buildRequest 按 Provider 分支：Claude 系在 system/长前缀消息注入 `cache_control`（仅 supportsPromptCaching 为 true 时）；OpenAI 兼容走自动缓存，不改请求体；未知 Provider 默认关闭并记录原因；
  2. 解析侧把 `cache_read_input_tokens`/`cached_tokens` 归一进 model-telemetry.js 既有的 normalizeCacheMetrics/mergeCacheMetrics；
  3. 指标落 model_call_metrics；**若现有列不足以承载缓存命中与节省费用，允许一次 Schema v17 增量迁移**——走 db-schema.js + migrator 既有模式（哈希校验、迁移状态、回滚路径），向后兼容 v16 数据，并新增 test/agent-runtime/schema-v17.test.js；DATA_MODEL.md 同步更新（23 张表 → 增量）；
  4. 设置面板账户详情显示「上次缓存命中/节省」只读摘要（数据缺失时显示 unknown，不伪装为 0——沿用 v1.1.3 确立的原则）。
- 验证：mock provider 单测覆盖三种 Provider 分支与无 usage 场景；若有真实 Claude Key，`npm run eval:one` 观察指标落库；v17 迁移用 v16 fixture 验证升级与回滚。

### F2 SQLite 测试 Electron 通道（消除 4+1 结构性跳过）
- 文件：新增 `scripts/run-sqlite-tests.js` + package.json `check:sqlite`；`.github/workflows/ci.yml` ui job 追加
- 现状：better-sqlite3 按 Electron ABI 编译，纯 Node `npm test` 下 storage-search-metrics 4 用例永远 skip；真实 DB 逻辑仅靠 check:electron-abi 的 `select 1` 冒烟。
- 改动：以 Electron 为主进程加载一个小 runner（机制候选：主进程内 `require('node:test')` 跑 storage 子集，或 `ELECTRON_RUN_AS_NODE=1` 方式执行），跑通全部 better-sqlite3 依赖用例并输出与 run-tests.js 同构的 JSON 摘要；CI 的 ui job（已有 Xvfb/Electron 环境）接入执行。
- 验证：本机 `npm run check:sqlite` 报 4 用例通过而非 skip；CI ui job 绿；`npm test` 在纯 Node 下行为不变（仍优雅跳过）。

---

## 全局注意点

1. **顺序建议**：A → B → C → D → F1 →（E 视进度）→ F2 → `node scripts/bump-version.js 1.1.5` → 完整 9 项 release gates（npm test / check:version / check:storage / check:perf / check:electron-abi / check:ui / bench:offline / check:release / git diff --check）→ 打包发布。
2. **Schema 策略**：默认零 Schema 变更；仅 F1 在列不足时做一次 v17 增量迁移，遵循既有 migrator 规范（哈希校验、状态记录、回滚），**不引入全局文档 RAG 索引**（v1.1.4 明确推迟项，留待后续版本与真实 Embedding Provider 一起评估）。
3. **红线不动**：API Key 仅存 OS 密钥库、明文不落盘；导出/备份/日志/Trace 持续脱敏；子 Agent 只读边界；loopback 鉴权语义不放宽。
4. **重构护栏**：D/E 两批为纯搬运，禁止行为、文案、错误码变化；每步以 464 测试 + ui-smoke 四窗口 + bench:offline 确定性输出（同 seed 字节一致）作验收。
5. **每批节奏**：一批一个 commit（沿用项目 conventional commits 风格），合入即跑 `npm test` + 该批验证项，绿了再进下一批；E 批若进度不足，整批顺延 v1.1.6，不挤占发版窗口。
6. 本计划为只读产物，各项改动待批准后由编码阶段按批实施。
