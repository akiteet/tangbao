# 糖绘 v1.1.5 升级计划（交互体验与 UI · 5 批 / 14 项）

> 来源：2026-08-16 对糖绘模块的全量调研（image.js 1029 行逐段阅读 + index.html/styles.css/状态层/能力层交叉核对）。
> 主线：**保持现有单列纵向布局与设计语言，修复交互断层 + 历史图片存储改造**（用户已确认：打磨现有布局、包含存储改造）。
> 设计依据：实际读取 image.js / index.html:165-168 / styles.css:661-833 / state.js:447-456 / image-capabilities.js 后形成；行号以 v1.1.5 工作树为准。
> 背景：v1.1.4 完成了图像模型能力适配（协议/尺寸/错误自学习），本计划聚焦其上的交互与 UI 层。

## 一、现状评估摘要

**健康面（不动）**：模型能力解析完善（image-capabilities 的 profile/learnFromError/chooseSize/adaptPayload）；参考图已支持点击/粘贴/拖拽三入口并自动压缩（image.js:307-377, 1005-1028）；串行队列带取消与重试（:667-699, 764-769）；灯箱/双图对比/下载/复制提示词基础能力齐备；错误触发的尺寸自学习（:472-492）。

**断层与债务（本计划依据）**：
1. **以图生图闭环断裂**：灯箱只有「重新生成/复制提示词/下载」（:903-957），生成结果无法一键变为编辑输入——参考图上传与结果展示是两个孤岛。
2. **切换视图丢输入**：onShow → render() 全量 innerHTML 重建（:193-195, 209-283），提示词与参考图草稿丢失。
3. **历史管理缺失 + 存储膨胀**：无删除/清空/搜索；图片以 base64 内联在 settings.imageHistory（30 条 × 4 张），每次持久化全量序列化进 state.json 与 SQLite。
4. **队列只有一行文字**：排队任务不可取消、无耗时显示（startedAt 记了未用）、队列与结果状态互写同一 #imgStatus 相互覆盖。
5. **灯箱无键盘/缩放**；网格 object-fit: cover 裁切竖图（styles.css:778 固定 220px）；骨架屏不随比例。
6. **预设命名用阻塞 window.prompt**（:421）；历史缩略图截断 4 张无展开（:854）；下载不按实际 MIME 命名（:965-971）。
7. **糖绘零媒体查询**：styles.css 全部 26 处 @media 均属其他模块。

## 依赖关系

- 批次 A/B/C 均为渲染层（image.js + styles.css）独立改动，可任意顺序；A1 是用户价值最高项建议先做。
- 批次 D 是地基项：D1 落盘改造改变历史数据形状，**D2/D3 与 C2（历史缩略图渲染）依赖 D1 的索引结构**，故 D1 先行、随后 D2/D3/C2 联调。
- 批次 E 收尾（窄屏适配需在布局细节稳定后做，避免反复）。
- 建议顺序：A → B → C(除 C2 历史部分) → D → C2 联调 → E。

---

## 批次 A — 交互闭环（低风险、最高 ROI，先行）

### A1 灯箱「用作参考图」：打通以图生图
- 文件：`src/renderer/views/images/image.js` openLightbox(:903-957)
- 现状：灯箱底部仅有 重新生成/复制提示词/下载；生成结果与参考图上传（refImage :191）完全断开，想编辑刚生成的图必须手动下载再上传。
- 改动：灯箱操作行新增「✎ 用作参考图」——将当前图（data URL）直接写入 refImage 并 showRef（复用既有 :307-377 的展示/移除逻辑），滚动定位到输入区并把按钮态切到「编辑图片」；同一入口对历史图片同样生效。附带新增「⧉ 复制图片」：canvas 转 blob → navigator.clipboard.write，失败 toast 提示改用下载。
- 验证：生成一张图 → 灯箱点「用作参考图」→ 输入区出现参考图缩略图、按钮变「编辑图片」；历史图同样可一键带入。

### A2 提示词与参考图草稿保留
- 文件：`image.js` render(:209-283)、onShow(:193-195)
- 现状：视图每次 onShow 全量重建，textarea 与参考图丢失（对比：糖码有 saveComposerDraft/restoreComposerDraft 先例）。
- 改动：仿糖码草稿模式——render() 开头保存 imgPrompt.value 与 refImage 到模块态（App.image._draft），渲染完成尾回填；参考图随草稿恢复。
- 验证：输入一半提示词 + 挂参考图 → 切到聊天再切回 → 内容与参考图均在；生成完成后草稿按现有行为清空提示词。

### A3 预设命名内联输入
- 文件：`image.js` :396-430（window.prompt 在 :421）
- 现状：阻塞式 window.prompt 命名预设，体验割裂且样式不可控。
- 改动：点击「＋存为预设」后在 chip 行原位展开小输入框（Enter 确认 / Esc 取消 / 失焦取消），沿用现有 presets 存储结构。
- 验证：保存预设全程无阻塞弹窗；Esc 不产生空名预设。

---

## 批次 B — 队列与生成反馈（低风险）

### B1 队列可视化任务卡
- 文件：`image.js` enqueue(:667-678)/pump(:680-699)/renderQueue(:778-792)
- 现状：队列并发 1，提示只有一行文字「正在生成…（队列剩余 N）」；排队中的任务不可取消（AbortController 只挂在运行任务上）；task.startedAt(:704) 已记录但从未展示。
- 改动：状态区下新增任务卡横条列表（最多显示最近 5 条）：每卡 = 缩略状态点 + prompt 前 20 字 + 比例/张数 + 状态（排队中第 N 位 / 已耗时 mm:ss 每秒刷新 / 失败 / 已取消）+ 操作（排队态可「移出」，失败态可「重试」，运行态沿用现有「取消」）。排队移出 = 从 queue 数组 splice。
- 验证：连发 3 个任务 → 三个任务卡可见、排队卡可移出、运行卡显示递增耗时、失败卡可重试。

### B2 骨架屏按比例渲染
- 文件：`image.js` renderSkeleton、`styles.css` .img-skeleton(:803-805)
- 现状：骨架卡固定 220px 等高，选 9:16 时骨架与结果形状不符。
- 改动：按当前所选尺寸的宽高比设置骨架卡 aspect-ratio（inline style），CSS 去掉固定高度改 min-height 兜底。
- 验证：切 16:9 与 9:16 各生成一次，骨架形状与结果一致。

### B3 状态区竞争修复
- 文件：`image.js` renderQueue(:778-792) 与 runTask finally（互写 #imgStatus）
- 现状：队列文案与结果/错误文案写同一节点，后者覆盖前者（或反之），多任务时信息丢失。
- 改动：拆为两个节点——#imgQueueStatus（队列态，由 renderQueue 独占）与 #imgStatus（最近一次任务结果，由 runTask 独占）；render() 增补第二个节点。
- 验证：队列运行中收到失败结果时，两行信息同时可见、互不覆盖。

---

## 批次 C — 灯箱与网格升级（低风险；C2 历史部分依赖 D1）

### C1 灯箱键盘与缩放
- 文件：`image.js` openLightbox(:903-957)、`styles.css` .img-lightbox(:821-824)
- 现状：无任何键盘监听；图片 max-height 64vh 无缩放。
- 改动：灯箱打开时挂 keydown（Esc 关闭 / ←→ 复用现有 prev/next），关闭即解绑；双击在 1x/2x 间切换、滚轮按 0.2 步进缩放（1x–4x，transform-origin 光标位置），拖拽平移放大态；底部条显示 100%/200% 缩放比。
- 验证：键盘三键可用；双击放大后拖拽查看细节；Esc 后再开无残留监听。

### C2 网格卡片真实比例渲染
- 文件：`styles.css` .img-card img(:778 固定 220px cover)、history 缩略图(:815-817)、`image.js` 卡片模板
- 现状：object-fit: cover 裁切竖图；历史缩略图固定 52px 方块同样裁切。
- 改动：结果网格与历史缩略图改为 aspect-ratio + object-fit: contain（或卡片按图片自然宽高比，加载后由 img 的 naturalWidth/Height 设 aspect-ratio，避免布局跳动：先按所选比例占位，onload 校正）；灯箱/对比不受影响。
- 验证：生成 9:16 与 16:9 混排无裁切、网格不塌；历史竖图完整可见。

### C3 下载命名与灯箱元信息
- 文件：`image.js` download(:965-971)、openLightbox
- 现状：一律存 .png（源可能是 jpeg）；灯箱不显示任何生成参数。
- 改动：按 data URL 的 mime 定扩展名（png/jpeg/webp）；灯箱底栏补一行元信息（模型 · 比例尺寸 · 时间），数据取自所属历史条目/最近任务。
- 验证：jpeg 源下载得到 .jpg；灯箱可看到该图的模型与时间。

---

## 批次 D — 历史存储改造与管理（高风险，含迁移，D1 先行）

### D1 图片落盘：base64 内联 → 数据根文件 + 轻量索引
- 文件：新增主进程 IPC（`src/main/main.js` 白名单通道 `image:saveAsset`/`image:readAsset`，仿既有 `image:fetchAsset` 于 preload:19 的模式）、`src/preload/preload.js`、`src/application/services/gateway.js`（或新增 image 资产 service）、`image.js` pushHistory/renderHistory、`src/renderer/state/state.js` imageHistory 默认值(:447)
- 现状：图片 base64 内联 settings.imageHistory（上限 30 条防膨胀），每次 App.persist 全量序列化进 state.json + SQLite；渲染历史时全部 base64 进 DOM。
- 改动：
  1. 新通道仅允许写入数据根 `tangbao-data/images/` 子目录（文件名服务端生成 `img-{ts}-{rand}.{ext}`，路径校验拒绝 `..` 与绝对路径）——渲染层继续不持有任意路径写权限（项目红线）；
  2. 历史条目改为轻量索引：`{ id, prompt, size, count, time, model, files: ['img-...png'] }`，不含 base64；
  3. 读回：history 渲染时按需经 `image:readAsset` 换取 data URL（内存 LRU 缓存最近 40 张）；
  4. 迁移：启动时扫描旧条目内 `data:` 图片 → 逐张落盘 → 全部成功后改写索引并清空旧字段 → App.persist；任一失败保留原状下次重试（先写文件后改索引，天然可重入回退）；
  5. 磁盘护栏：images 目录超 500MB 时新图不再落盘并 toast 提示清理。
- 验证：迁移后 state.json 体积显著下降、历史图片正常显示；删除 images 目录任一文件后对应缩略图显示占位不崩溃；卸载重装数据不丢（数据根仍在）。

### D2 历史管理：删除 / 清空 / 搜索
- 文件：`image.js` renderHistory(:835-871)
- 现状：无任何管理操作，30 条全量渲染。
- 改动：条目悬浮操作「🗑 删除」（落盘文件一并删除，经同一通道的 `image:deleteAsset`）与顶部「清空历史」（确认弹窗，删除全部文件）；搜索框按提示词子串过滤（前端内存过滤即可）；上限 30 → 200 条（依赖 D1 落盘后的轻量索引）。
- 验证：删单条后列表与磁盘同步；清空需确认且可取消；搜索即时过滤。

### D3 历史条目「查看全部」
- 文件：`image.js` renderHistory(:854 截断 slice(0,4))
- 现状：每条最多 4 张缩略图，多的无处可看。
- 改动：超过 4 张时尾部显示「+N」角标块，点击展开该条全部缩略图（行内展开，非弹窗）。
- 验证：生成 4 张 × 2 次后历史条目可展开 8 张。

---

## 批次 E — 窄屏适配与对比增强（收尾）

### E1 糖绘专属媒体查询
- 文件：`styles.css`（糖绘区块 L661-833 内追加 @media）
- 现状：糖绘零媒体查询；窄窗口下参数标签 40px 挤压、卡片 220px 过高、高级参数两列不分栏、灯箱按钮偏小。
- 改动：≤900px：.opt-label 上置（label 与 chip 行改纵向）、.img-card 高度上限下调、.adv-grid 单列、灯箱 prev/next 加大命中区；≤560px：结果网格最小卡宽 140px、历史缩略图换行。
- 验证：`npm run check:ui` 四窗口（desktop/small-desktop/compact/narrow）糖绘截图无挤压破版。

### E2 对比升级：任选图片 + 参数差异
- 文件：`image.js` compareList(:190)、openCompare(:874-901)、renderHistory(:862-868)
- 现状：只能选 2 条历史、且各取第 1 张图（:865 e.images[0]）；对比弹窗无参数信息。
- 改动：选择粒度从「历史条目」改为「单张图片」（缩略图角标勾选，选满 2 张自动弹）；对比弹窗每栏下方显示 prompt/模型/比例/时间，差异字段高亮（不同参数标色）。
- 验证：同一批 4 张中任选 2 张可对比；比例不同的两图可见高亮差异。

---

## 全局注意点

1. **红线不动**：渲染层不持有任意路径读写权限——所有文件操作走主进程白名单通道且限定 `images/` 子目录；API Key 处理不涉及。
2. **Schema 保持 v16**：历史索引继续走 settings（轻量化后无膨胀问题），不新增表；迁移不触碰 SQLite 结构。
3. **每批节奏**：一批一 commit（conventional commits），合入即跑 `npm test` + `npm run check:ui`，绿了再进下一批；D 批额外补单测（迁移幂等、路径校验、配额护栏）。
4. **版本**：本计划按用户指示并入当前 v1.1.5 发布分支实施；发版沿用 `npm run bump` 与 10 项 release gates。
5. **性能顺带收益**：D1 落盘后 state 序列化与历史渲染的 base64 全量进 DOM 问题一并消除（这也与 v1.1.5 遗留的「切换卡顿」排查方向部分重叠，可在实施 D 后复测切换手感）。
6. 本计划为只读产物，各项改动待批准后由编码阶段按批实施。

---

## 附：实施状态（2026-08-16，并入 v1.1.5 发布分支）

- 批次 A（交互闭环）：✅ A1 灯箱「用作参考图/复制图片」、A2 草稿保留（提示词+参考图）、A3 预设内联输入全部落地。
- 批次 B（队列反馈）：✅ B1 任务卡（排队可移出/运行耗时/失败重试/取消态，1s 计时刷新）、B2 骨架屏按比例、B3 队列与结果状态分节点显示。
- 批次 C（灯箱与网格）：✅ C1 键盘+双击/滚轮缩放+拖拽平移、C2 真实比例渲染（占位→onload 校正）、C3 MIME 嗅探下载命名+灯箱元信息。
- 批次 D（存储改造）：✅ 新增 storage/image-assets.js（可测原语：嗅探/白名单校验/配额）+ 主进程三通道 IPC + services/images.js；pushHistory 落盘优先、失败回退内联；启动迁移幂等；删除/清空（非阻塞确认）/搜索/查看全部；上限 200。state.js 归一化同步接受 files 新格式。
- 批次 E（窄屏与对比）：✅ ≤900px/≤560px 两档媒体查询；对比升级为单张粒度（⊞ 角标）+ 模型/比例/时间差异高亮，条目级「对比」保留为快捷入口。
- 验证：image-assets 单测 6/6；npm test 478 用例 / 473 通过 / 0 失败；check:ui 四窗口通过。
